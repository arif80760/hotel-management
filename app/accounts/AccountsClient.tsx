"use client";

// app/accounts/AccountsClient.tsx
// Accounts daybook — client component.
//
// STAGE 2 PLACEHOLDER. This is the route + guard scaffold only.
// The real daybook (balance cards, transaction list, filters,
// manual entry form) is built in the following Stage 2 steps.
// accountsService is intentionally not wired up yet.

export default function AccountsClient() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-slate-800">Accounts</h1>
      <p className="mt-2 text-sm text-slate-500">
        Daybook — coming soon.
      </p>
    </div>
  );
}
